package thx.xml;

import utest.Assert;
using thx.xml.Document;

class TestHaxeNativeXML {
  public function new() {}

  // public function testFromXml() {
  //   var xml = Xml.parse('<doc><child attr="value">text</child></doc>'),
  //       doc = xml.toDom4();
  //   Assert.notNull(doc.documentElement);
  //   Assert.equals("doc", doc.documentElement.tagName);
  //   Assert.equals(1, doc.documentElement.childElementCount);
  //   var child = doc.documentElement.children[0];
  //   Assert.equals("child", child.tagName);
  //   Assert.equals("value", child.getAttribute("attr"));
  //   Assert.equals("text", child.textContent);
  // }
}
